import { Button } from './Button'

export default {
  title: 'Finora/Atoms/Button',
  component: Button,
}

export const Primary = {
  args: { children: 'Get notified', variant: 'primary' },
}

export const Secondary = {
  args: { children: 'Learn more', variant: 'secondary' },
}
